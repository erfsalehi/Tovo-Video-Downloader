diff --git a/app.py b/app.py
index 14691b1..8565f88 100644
--- a/app.py
+++ b/app.py
@@ -113,13 +113,12 @@ def render_classic_mode():
         audio_file = st.file_uploader("Upload Voiceover (.mp3, .wav, .m4a)", type=["mp3", "wav", "m4a", "mp4"])
 
     if script_file and audio_file:
-        # Read files
-        script_content = script_file.read().decode("utf-8")
+        # Use getvalue() so reruns don't consume the buffer.
+        script_content = script_file.getvalue().decode("utf-8")
 
-        # Save audio temporarily to read duration
         audio_path = os.path.join(".cache", "temp_audio" + os.path.splitext(audio_file.name)[1])
         with open(audio_path, "wb") as f:
-            f.write(audio_file.read())
+            f.write(audio_file.getvalue())
 
         duration = get_audio_duration(audio_path)
         word_count = len(script_content.split())
@@ -436,12 +435,12 @@ elif app_mode == "Director (v0.2)":
         audio_file = st.file_uploader("Upload Voiceover (to extract duration)", type=["mp3", "wav", "m4a"], key="d_audio")
         
     if script_file and audio_file:
-        script_content = script_file.read().decode("utf-8")
-        
+        script_content = script_file.getvalue().decode("utf-8")
+
         audio_path = os.path.join(".cache", "temp_audio_director" + os.path.splitext(audio_file.name)[1])
         with open(audio_path, "wb") as f:
-            f.write(audio_file.read())
-            
+            f.write(audio_file.getvalue())
+
         st.session_state.script_text = script_content
         st.session_state.audio_duration = get_audio_duration(audio_path)
         
diff --git a/core/keywords.py b/core/keywords.py
index 27d19f1..dcd2395 100644
--- a/core/keywords.py
+++ b/core/keywords.py
@@ -3,6 +3,17 @@ import re
 from groq import Groq
 from tenacity import retry, wait_exponential, stop_after_attempt, retry_if_exception_type
 
+# Only retry transient errors. Auth / bad-request failures should fail fast.
+try:
+    from groq import APIConnectionError, RateLimitError, InternalServerError
+    _RETRYABLE_GROQ_ERRORS = (APIConnectionError, RateLimitError, InternalServerError)
+except ImportError:  # older groq SDKs
+    _RETRYABLE_GROQ_ERRORS = ()
+
+import requests as _requests
+_RETRYABLE = _RETRYABLE_GROQ_ERRORS + (_requests.RequestException, TimeoutError)
+
+
 def load_prompt() -> str:
     prompt_path = os.path.join(os.path.dirname(__file__), '..', 'prompts', 'visual_keywords.txt')
     with open(prompt_path, 'r', encoding='utf-8') as f:
@@ -11,7 +22,7 @@ def load_prompt() -> str:
 @retry(
     wait=wait_exponential(multiplier=1, min=2, max=10),
     stop=stop_after_attempt(5),
-    retry=retry_if_exception_type(Exception)
+    retry=retry_if_exception_type(_RETRYABLE)
 )
 def call_groq_api(client: Groq, system_prompt: str, user_content: str) -> str:
     response = client.chat.completions.create(
@@ -71,43 +82,35 @@ def generate_keywords_for_slots(slots: list, api_key: str, num_alternatives: int
 def parse_groq_response(response_text: str, original_batch: list, num_alternatives: int):
     """
     Parses the response and mutates the dictionaries in original_batch to include 'keywords' list.
+
+    Slots are assigned by ORDER of appearance in the response, not by timestamp
+    string match. This avoids two slots within the same second colliding on the
+    same `[HH:MM:SS]` key.
     """
     for slot in original_batch:
         if 'keywords' not in slot:
             slot['keywords'] = []
-            
-    lines = response_text.strip().split('\n')
-    current_ts = None
-    current_keywords = []
-    
-    ts_to_slot = {format_time(s['timestamp']): s for s in original_batch}
-    
-    for line in lines:
+
+    blocks = []  # list of keyword lists, one per `[HH:MM:SS]` marker found
+    current_keywords = None
+
+    for line in response_text.strip().split('\n'):
         line = line.strip()
         if not line:
             continue
-            
-        # Match timestamp exactly like [00:00:00]
-        ts_match = re.match(r'^\[(\d{2}:\d{2}:\d{2})\]', line)
-        if ts_match:
-            if current_ts and current_ts in ts_to_slot:
-                ts_to_slot[current_ts]['keywords'] = current_keywords[:num_alternatives]
-            current_ts = ts_match.group(0)
+
+        if re.match(r'^\[(\d{2}:\d{2}:\d{2})\]', line):
             current_keywords = []
-        else:
-            if current_ts:
-                # Remove common list prefixes AI might add (e.g. "1.", "- ", "* ")
-                clean_line = re.sub(r'^[\d\.\-\*]+\s*', '', line).strip()
-                # Remove quotes if AI quoted the keywords
-                clean_line = clean_line.strip('"\'')
-                if clean_line:
-                    current_keywords.append(clean_line)
-                    
-    # Handle the final batch item
-    if current_ts and current_ts in ts_to_slot:
-        ts_to_slot[current_ts]['keywords'] = current_keywords[:num_alternatives]
-        
-    # Fallback if any slot is missing keywords (e.g. AI skipped it)
+            blocks.append(current_keywords)
+        elif current_keywords is not None:
+            clean_line = re.sub(r'^[\d\.\-\*]+\s*', '', line).strip()
+            clean_line = clean_line.strip('"\'')
+            if clean_line:
+                current_keywords.append(clean_line)
+
+    for slot, kws in zip(original_batch, blocks):
+        slot['keywords'] = kws[:num_alternatives]
+
     for slot in original_batch:
         if not slot['keywords']:
             slot['keywords'] = [f"No keywords generated for {slot['text']}"]
diff --git a/core/output.py b/core/output.py
index c983ce7..bfafa41 100644
--- a/core/output.py
+++ b/core/output.py
@@ -81,31 +81,68 @@ def generate_shot_list_txt(shots: list) -> str:
         lines.append("")
     return "\n".join(lines)
 
+def _xml_escape(s: str) -> str:
+    return (
+        s.replace('&', '&amp;')
+         .replace('<', '&lt;')
+         .replace('>', '&gt;')
+         .replace('"', '&quot;')
+         .replace("'", '&apos;')
+    )
+
+
 def generate_fcpxml(shots: list) -> str:
-    # A basic FCPXML wrapper for markers
-    xml = ['<?xml version="1.0" encoding="UTF-8"?>']
-    xml.append('<!DOCTYPE fcpxml>')
-    xml.append('<fcpxml version="1.9">')
-    xml.append('  <library>')
-    xml.append('    <event name="B-Roll Director Markers">')
-    xml.append('      <project name="Generated Markers">')
-    xml.append('        <sequence format="r1" tcStart="0s" tcFormat="NDF">')
-    xml.append('          <spine>')
-    xml.append('            <gap name="Master" duration="3600s" start="0s">')
-    
+    """
+    Build a minimal but valid FCPXML 1.9 marker file.
+
+    Final Cut Pro requires every `format` referenced by id to be declared
+    inside `<resources>`. The previous implementation referenced `r1` without
+    declaring it, so FCP refused the import. Sequence/gap durations are also
+    sized to the actual marker range instead of a hardcoded 3600s.
+    """
+    # Compute total duration covering all markers, with at least 1s padding.
+    end_sec = max(
+        (int(shot.get('end_timestamp', shot.get('timestamp', 0) + 1)) for shot in shots),
+        default=1,
+    )
+    total_duration = max(end_sec + 1, 1)
+
+    xml = [
+        '<?xml version="1.0" encoding="UTF-8"?>',
+        '<!DOCTYPE fcpxml>',
+        '<fcpxml version="1.9">',
+        '  <resources>',
+        '    <format id="r1" name="FFVideoFormat1080p30" frameDuration="100/3000s" '
+        'width="1920" height="1080" colorSpace="1-1-1 (Rec. 709)"/>',
+        '  </resources>',
+        '  <library>',
+        '    <event name="B-Roll Director Markers">',
+        '      <project name="Generated Markers">',
+        f'        <sequence format="r1" duration="{total_duration}s" tcStart="0s" tcFormat="NDF">',
+        '          <spine>',
+        f'            <gap name="Master" offset="0s" duration="{total_duration}s" start="0s">',
+    ]
+
     for shot in shots:
-        start_sec = shot.get('timestamp', 0)
-        intent = shot.get('shot_intent', '').replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;')
-        queries = " | ".join(shot.get('search_queries', [])).replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;')
-        marker_name = f"{intent} ({queries})"
-        
-        xml.append(f'              <marker start="{start_sec}s" duration="1s" value="{marker_name}"/>')
-        
-    xml.append('            </gap>')
-    xml.append('          </spine>')
-    xml.append('        </sequence>')
-    xml.append('      </project>')
-    xml.append('    </event>')
-    xml.append('  </library>')
-    xml.append('</fcpxml>')
+        start_sec = int(shot.get('timestamp', 0))
+        end_sec_shot = int(shot.get('end_timestamp', start_sec + 1))
+        marker_dur = max(end_sec_shot - start_sec, 1)
+
+        intent = _xml_escape(shot.get('shot_intent', ''))
+        queries = _xml_escape(" | ".join(shot.get('search_queries', [])))
+        marker_name = f"{intent} ({queries})" if queries else intent
+
+        xml.append(
+            f'              <marker start="{start_sec}s" duration="{marker_dur}s" value="{marker_name}"/>'
+        )
+
+    xml.extend([
+        '            </gap>',
+        '          </spine>',
+        '        </sequence>',
+        '      </project>',
+        '    </event>',
+        '  </library>',
+        '</fcpxml>',
+    ])
     return "\n".join(xml)
diff --git a/core/youtube.py b/core/youtube.py
index 7a371b6..4f4a351 100644
--- a/core/youtube.py
+++ b/core/youtube.py
@@ -11,10 +11,10 @@ def search_youtube_single(keyword: str, num_shorts: int = 0, num_longs: int = 3)
     """
     ydl_opts = {
         'extract_flat': True,
-        'force_generic_extractor': True,
         'quiet': True,
         'no_warnings': True,
-        'simulate': True
+        'simulate': True,
+        'default_search': 'ytsearch',
     }
     
     if not keyword or keyword.startswith("Error:") or keyword.startswith("No keywords generated"):
@@ -142,9 +142,14 @@ def download_video(url: str, output_path: str, quality: str, task_state: dict):
                 downloaded_bytes = d.get('downloaded_bytes', 0)
                 task_state['progress'] = downloaded_bytes / total_bytes
                 
+    # yt-dlp expects an extension placeholder; pass the path stem and let it
+    # pick the extension. merge_output_format ensures the final file is .mp4.
+    base, _ = os.path.splitext(output_path)
+    outtmpl = base + '.%(ext)s'
+
     ydl_opts = {
         'format': ydl_format,
-        'outtmpl': output_path,
+        'outtmpl': outtmpl,
         'progress_hooks': [my_hook],
         'quiet': True,
         'no_warnings': True,

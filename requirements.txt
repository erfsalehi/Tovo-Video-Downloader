# Core downloader
yt-dlp>=2024.8.6

# Optional: explicit certificate bundle for SSL verification.
# Recommended on locked-down corporate networks.
# certifi

# Network requests (curl/requests fallback path + shared HTTP)
requests>=2.31.0

# Groq cloud Whisper — official SDK (httpx/HTTP-2 transport). Preferred over the
# raw requests/curl upload because Cloudflare accepts its TLS fingerprint in
# regions where requests gets reset ('SSL: UNEXPECTED_EOF'). httpx ships with it
# but is listed since we build the proxy-aware client directly.
groq>=1.0.0
httpx>=0.27.0

# Short Clips: rounded-pill burned captions with correct Persian/Arabic shaping.
# Pillow renders the caption images; the other two shape + reorder RTL text.
Pillow>=9.2.0
arabic_reshaper>=3.0.0
python-bidi>=0.4.2

# System dependencies (install separately):
#   - ffmpeg (required)
#   - deno   (required by yt-dlp for some YouTube extractors)

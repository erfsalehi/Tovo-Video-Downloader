# Core downloader
yt-dlp>=2024.8.6

# Whisper AI smart-sync (optional but recommended).
# Loaded lazily; downloads will still work without it.
stable-ts>=2.17.0

# Torch is installed separately on Windows by Start.bat (CPU-only build, ~200MB).
# On Linux/macOS, install manually if you want Whisper sync:
#   pip install torch torchaudio
# torch>=2.0.0
# torchaudio>=2.0.0

# Optional: explicit certificate bundle for SSL verification.
# Recommended on locked-down corporate networks.
# certifi

# Network requests for Groq API
requests>=2.31.0

# Short Clips: rounded-pill burned captions with correct Persian/Arabic shaping.
# Pillow renders the caption images; the other two shape + reorder RTL text.
Pillow>=9.2.0
arabic_reshaper>=3.0.0
python-bidi>=0.4.2

# System dependencies (install separately):
#   - ffmpeg (required)
#   - deno   (required by yt-dlp for some YouTube extractors)
